import 'package:flutter/material.dart';

class WorkshopColors {
  // Step 2: Change the colors to whatever colors you think look nice for your app!
  static const Color primary = Color(0xFF7D0AFF);
  static const Color background = Color(0xFF1A0E41);
  static const Color white = Color(0xFFFFFFFF);
  static const Color secondary = Color(0xFF05D4FE);
}
